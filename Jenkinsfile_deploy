pipeline {
  agent any
  
  stages {
    stage('deploy to tomcat') {
      steps {
        sshagent(['test_user']) {
          sh 'sudo scp -o StrictHostKeyChecking=no /var/lib/jenkins/workspace/JavaMaven_Pipeline_Job/webapp/target/*.war ec2-user@13.233.123.36:/usr/share/tomcat/webapps'
        }
      }
     }
  }
}
